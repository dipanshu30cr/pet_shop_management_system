@model IEnumerable<pet_shop_management_system.Models.breed>

@{
    ViewBag.Title = "Index";
}
<style>
    body {
        background-image: url('/Content/Images/back_2.jpg');
        background-size: cover; /* Adjust the background-size property as needed */
    }
</style>

<h2>Index</h2>
<p style="background-image: url('~/Content/Images/login_background.jpg');">
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.breed_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.breed_description)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.breed_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.breed_description)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.id }) |
                @Html.ActionLink("Details", "Details", new { id = item.id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.id })
            </td>
        </tr>
    }

</table>
